services:
  - type: web
    name: annumovies-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TMDB_TOKEN
        sync: false          # Set manually in Render dashboard
      - key: NODE_ENV
        value: production
    autoDeploy: true
